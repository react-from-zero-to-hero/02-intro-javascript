// Operador ternario

const activo = true;

// let mensaje;

// if ( activo ) {
//     mensaje = 'activo';
// } else {
//     mensaje = 'inactivo';
// }


// const mensaje = ( activo ) ? 'Activo' : 'mensaje';

const mensaje = activo && 'Activo';

console.log( mensaje );

// en una condición AND si el primero es false entonces
// la siguiente no se ejecuta.
